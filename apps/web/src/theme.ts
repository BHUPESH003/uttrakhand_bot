/**
 * Re-exported from packages/theme, the single source of truth shared with
 * apps/admin — see that package for the actual values. Kept as a local
 * module so every existing `@/theme` import in this app keeps working.
 */
export { theme } from "theme";
