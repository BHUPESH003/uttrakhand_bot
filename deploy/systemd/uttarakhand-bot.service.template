[Unit]
Description=Uttarakhand e-Seva WhatsApp Bot
After=network.target

[Service]
Type=simple
User=${SERVICE_USER}
WorkingDirectory=${REPO_ROOT}
Environment=NODE_ENV=production
ExecStart=${PNPM_BIN} --filter bot start
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
